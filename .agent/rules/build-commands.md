---
trigger: always_on
---

use this command to build the server "./gradlew server:build"

use this command to build the webApp " cmd /c "npm run build" "

use this command to build the shared module "./gradlew :shared:jsBrowserDevelopmentLibraryDistribution"