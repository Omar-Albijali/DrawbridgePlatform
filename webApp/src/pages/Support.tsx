import PageShell from '../components/PageShell';

export default function Support(): JSX.Element {
  return <PageShell title="Support" description="Contact support options and issue reporting will render here." />;
}
