import PageShell from '../components/PageShell';

export default function Checkout(): JSX.Element {
  return <PageShell title="Checkout" description="Shipping and payment flow UI lands during page migration." />;
}
