import PageShell from '../components/PageShell';

export default function Inventory(): JSX.Element {
  return <PageShell title="Inventory" description="Inventory management tools are staged for page migration." />;
}
