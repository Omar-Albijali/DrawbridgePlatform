import PageShell from '../../components/PageShell';

export default function Profile(): JSX.Element {
  return <PageShell title="Profile Settings" description="Manage profile identity and contact information." />;
}
