import PageShell from '../../components/PageShell';

export default function Security(): JSX.Element {
  return <PageShell title="Security Settings" description="Control password, MFA, and session policies." />;
}
