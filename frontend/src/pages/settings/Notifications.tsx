import PageShell from '../../components/PageShell';

export default function Notifications(): JSX.Element {
  return <PageShell title="Notification Settings" description="Tune email and in-app notification preferences." />;
}
