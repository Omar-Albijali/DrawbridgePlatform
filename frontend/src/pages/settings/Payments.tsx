import PageShell from '../../components/PageShell';

export default function Payments(): JSX.Element {
  return <PageShell title="Payment Settings" description="Configure payment methods and billing defaults." />;
}
