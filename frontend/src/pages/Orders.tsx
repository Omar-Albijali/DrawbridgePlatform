import PageShell from '../components/PageShell';

export default function Orders(): JSX.Element {
  return <PageShell title="Orders" description="Order list and status filters will be added in the next task." />;
}
