import PageShell from '../components/PageShell';

export default function Cart(): JSX.Element {
  return <PageShell title="Cart" description="Cart line items and totals are connected through CartContext." />;
}
